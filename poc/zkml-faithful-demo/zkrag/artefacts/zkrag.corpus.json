{
  "model": "sentence-transformers/all-MiniLM-L6-v2 (REAL trained weights, masked mean pool; semantic embeddings)",
  "semantic": true,
  "semantic_fallback": false,
  "semantic_fallback_reason": "",
  "tokenizer": "WordPiece AutoTokenizer (real)",
  "onnx": "minilm-l6-trained.onnx",
  "embed_backend": "rocm",
  "embed_fallback": false,
  "embed_doc_ms": 9.345,
  "embed_query_ms": 1.407,
  "seq_len": 32,
  "hidden": 384,
  "d": 32,
  "val_range": 127,
  "n": 32,
  "docs": [
    "zero knowledge proofs let a verifier check a computation without re-running it",
    "a STARK proof is transparent and needs no trusted setup",
    "Groth16 is a succinct pairing based SNARK with a trusted setup",
    "the RISC0 zkVM proves the execution trace of a RISC-V program",
    "recursion folds many proofs into one accumulator instance",
    "polynomial commitments bind a prover to a polynomial they chose",
    "the AMD Strix Halo APU pairs Zen 5 cores with a Radeon iGPU",
    "unified LPDDR5X memory is shared coherently between the CPU and the iGPU",
    "ROCm brings the gfx1151 iGPU up for PyTorch and MIGraphX inference",
    "the XDNA2 NPU accelerates AI model prefill but never the ZK proof",
    "94 GB of unified memory removes the 16 GB discrete GPU out of memory wall",
    "Rayon thread scaling drives the CPU only STARK prover across 32 threads",
    "retrieval augmented generation grounds a language model in fetched documents",
    "an HNSW graph navigates approximate nearest neighbours greedily",
    "the embedding model maps a sentence to a dense vector",
    "cosine similarity ranks candidate documents against the query vector",
    "a vector database stores millions of embeddings for fast search",
    "MiniLM is a small distilled transformer used for sentence embeddings",
    "quantising float embeddings to integers keeps the proof arithmetic exact",
    "the verifier learns only the committed digest of the private index",
    "a priority queue keeps the best-first frontier during graph search",
    "membership proofs show a returned id really belongs to the index",
    "top k recall measures how many true neighbours the search recovered",
    "the journal commits the query digest and the returned neighbour ids",
    "MIGraphX compiles the ONNX graph for the AMD gpu target",
    "onnxruntime runs the same ONNX on the CPU execution provider",
    "the iGPU forward pass beats the CPU on compute bound dense GEMM",
    "memory bound MSM only reaches CPU parity at large sizes on the iGPU",
    "a Merkle commitment lets the prover open one leaf at a time",
    "sumcheck reduces a claim about a sum to a claim at a random point",
    "lookup arguments prove that every value came from a fixed table",
    "the capstone proves an iGPU embedded retrieval was honest on the CPU"
  ],
  "query": "how does the AMD iGPU accelerate embeddings while the proof stays on the CPU"
}